<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boot.infopass.mapper.MyPageUserMapperInter">
  <select id="findUserById" parameterType="int" resultType="boot.infopass.dto.MyPageUserDto">
    SELECT id, name, email, exp, level, created_at AS createdAt
    FROM User
    WHERE id = #{id}
  </select>
</mapper>
