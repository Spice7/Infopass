<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.infopass.mapper.UserMapper">

    <!-- UserDto 매핑-->
    <resultMap type="UserDto" id="userMap">
        <id property="id" column="id" />
        
        <result property="id" column="id" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="nickname" column="nickname" />
        <result property="phone" column="phone" />
        <result property="address" column="address" />
        <result property="usertype" column="usertype" />
        <result property="exp" column="exp" />
        <result property="level" column="level" />
        <result property="rank_updated_at" column="rank_updated_at" />
        <result property="created_at" column="created_at" />
        
        
        <collection property="authList" resultMap="authMap"></collection>
    </resultMap>
	    <!-- 사용자 상세 정보 조회 (UserDto 반환) -->
    <select id="findByEmail" parameterType="String" resultType="user">
        SELECT * FROM User WHERE email = #{email}
    </select>
	<select id="existsByEmail" parameterType="String" resultType="boolean">
		select count(*) from User where email=#{email}
	</select>	
	<insert id="insertUser" parameterType="user">
		insert into User(password, name, nickname, email, phone, address, rank_updated_at, created_at) values(#{password}, #{name}, #{nickname}, #{email}, #{phone}, #{address}, now(), now())
	</insert>
</mapper>