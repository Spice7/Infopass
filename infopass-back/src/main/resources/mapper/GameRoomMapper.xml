<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.infopass.mapper.GameRoomMapper">
    <insert id="insertRoom" parameterType="GameRoomDto"> insert into game_room(room_name,
        max_players, status, created) values (#{roomName}, #{maxPlayers}, #{status}, NOW()) </insert>
    <select id="selectRoom" resultType="GameRoomDto"> select * from game_room where id=#{id} </select>
    <select id="selectAllRooms" resultType="GameRoomDto"> SELECT id, room_name AS roomName,
        max_players AS maxPlayers, status, created AS createdAt FROM game_room </select>
    <update id="updateRoomStatus"> update game_room set status=#{status} where id=#{id} </update>

</mapper>