<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.infopass.mapper.MultiplayerMapper">
	<insert id="insertMultiplayer" parameterType="Multiplayer">
		insert into multiplayer_rank (user_id, updated_at) values(#{user_id},now())
	</insert>
</mapper>
